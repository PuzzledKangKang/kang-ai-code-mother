spring:
  application:
    name: kang-ai-code-mother-backend
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: net.pkk.kangaicodemother.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
